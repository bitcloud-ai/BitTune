{
  "additionalProperties": false,
  "description": "Structured test strategy proposed by the remote model.",
  "properties": {
    "profiles": {
      "items": {
        "type": "string"
      },
      "maxItems": 4,
      "minItems": 1,
      "title": "Profiles",
      "type": "array"
    },
    "rationale": {
      "maxLength": 4096,
      "minLength": 1,
      "title": "Rationale",
      "type": "string"
    },
    "schema_version": {
      "default": "benchmark-intent/v1",
      "title": "Schema Version",
      "type": "string"
    }
  },
  "required": [
    "profiles",
    "rationale"
  ],
  "title": "BenchmarkIntent",
  "type": "object"
}
